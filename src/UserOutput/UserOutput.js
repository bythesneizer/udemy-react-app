import React from 'react'

const userOutput = (props) => {
    return(
        <div>
            <p>{props.username}</p>
            <p>{props.p2}</p>
        </div>
    )
}

export default userOutput